    import XCTest
    @testable import RepoAbstraction

    final class RepoAbstractionTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(RepoAbstraction().text, "Hello, World!")
        }
    }
