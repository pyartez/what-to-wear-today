    import XCTest
    @testable import W2W_Presenter

    final class W2W_PresenterTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(W2W_Presenter().text, "Hello, World!")
        }
    }
